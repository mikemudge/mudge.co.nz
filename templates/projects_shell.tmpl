<html>
<head lang="en">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1, minimum-scale=1.0, maximum-scale=1.0">

  <title>{{ title }}</title>
  <link rel="stylesheet" type="text/css" href="/static/projects/projects.css" />
</head>
<body>
  {% if back_link %}
    <p><a href="{{ back_link }}">&larr; {{ back_link_label }}</a></p>
  {% endif %}

  <div id="apps-list" data-status="{{ status }}" data-show-apps="{{ show_apps }}"></div>

  <script src="/static/projects/apps.js"></script>
</body>
</html>
