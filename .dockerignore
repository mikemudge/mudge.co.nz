# Not needed at runtime - local dev (docker-compose.yml) bind-mounts the
# repo checkout over the image anyway, so none of this needs to be baked in.
.git
node_modules
.local_postgres
tests

# Never bake secrets into an image layer.
settings/local_config.py

# Runtime-generated, not meaningful at build time.
.commithash

__pycache__
*.py[cod]
.idea
.DS_Store
.venv*
venv
